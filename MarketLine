<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>marketplace Usati e Nuovi</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <h1>Marketplace Usati e Nuovi</h1>
        <nav>
            <ul>
                <li><a href="#home">Home</a></li>
                <li><a href="#account">Account</a></li>
                <li><a href="#categorie">Categorie</a></li>
                <li><a href="#vendita">Vendi</a></li>
                <li><a href="#offerte">Offerte</a></li>
                <li><a href="#admin">Gestione Admin</a></li>
                <li><a href="#chat">Chat</a></li>
            </ul>
        </nav>
        <input type="text" placeholder="Cerca prodotti..." id="search">
    </header>
    <main>
        <section id="home">
            <h2>Compra e Vendi Online</h2>
            <p>Trova oggetti nuovi e usati a prezzi convenienti!</p>
        </section>
        <section id="chat">
            <h2>Chat Messaggistica</h2>
            <div id="chatWindow"></div>
            <input type="text" id="chatInput" placeholder="Scrivi un messaggio...">
            <button onclick="sendMessage()">Invia</button>
        </section>
        <section id="account">
            <h2>Accedi / Registrati</h2>
            <form id="loginForm">
                <input type="email" id="email" placeholder="Email" required>
                <input type="password" id="password" placeholder="Password" required>
                <button type="submit">Accedi</button>
            </form>
            <p id="loginMessage"></p>
        </section>
        <section id="vendita">
            <h2>Vendi un prodotto</h2>
            <form id="sellProductForm">
                <input type="text" id="productName" placeholder="Nome prodotto" required>
                <input type="number" id="productPrice" placeholder="Prezzo" required>
                <textarea id="productDescription" placeholder="Descrizione" required></textarea>
                <h3>Metodo di Pagamento</h3>
                <select id="paymentMethod" required>
                    <option value="paypal">PayPal</option>
                    <option value="visa">Visa</option>
                    <option value="mastercard">MasterCard</option>
                    <option value="twint">Twint</option>
                </select>
                <button type="submit">Metti in vendita</button>
            </form>
        </section>
        <section id="categorie">
            <h2>Categorie di prodotti</h2>
            <ul>
                <li>Elettronica</li>
                <li>Moda</li>
                <li>Casa</li>
                <li>Sport</li>
                <li>Auto e Moto</li>
            </ul>
        </section>
        <section id="offerte">
            <h2>Prodotti in Offerta</h2>
        </section>
        <section id="admin">
            <h2>Gestione Admin</h2>
            <p>Visualizza e gestisci i prodotti in vendita.</p>
            <ul id="productList"></ul>
        </section>
    </main>
    <footer>
        <p>&copy; 2025 Marketplace Usati e Nuovi</p>
    </footer>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const loginForm = document.getElementById('loginForm');
            const sellProductForm = document.getElementById('sellProductForm');
            const productList = document.getElementById('productList');
            const chatWindow = document.getElementById('chatWindow');
            const chatInput = document.getElementById('chatInput');
            let products = [];
            let messages = [];
            
            if (loginForm) {
                loginForm.addEventListener('submit', function(event) {
                    event.preventDefault();
                    const email = document.getElementById('email').value;
                    const password = document.getElementById('password').value;
                    if(email && password) {
                        document.getElementById('loginMessage').innerText = `Benvenuto, ${email}!`;
                    } else {
                        document.getElementById('loginMessage').innerText = 'Inserisci credenziali valide!';
                    }
                });
            }
            
            if (sellProductForm) {
                sellProductForm.addEventListener('submit', function(event) {
                    event.preventDefault();
                    const name = document.getElementById('productName').value;
                    const price = document.getElementById('productPrice').value;
                    const description = document.getElementById('productDescription').value;
                    const paymentMethod = document.getElementById('paymentMethod').value;
                    
                    if (name && price && description && paymentMethod) {
                        const product = { name, price, description, paymentMethod };
                        products.push(product);
                        updateProductList();
                        alert("Prodotto messo in vendita con successo!");
                    } else {
                        alert("Compila tutti i campi per vendere il prodotto.");
                    }
                });
            }
            
            function updateProductList() {
                productList.innerHTML = '';
                products.forEach((product, index) => {
                    const li = document.createElement('li');
                    li.innerHTML = `${product.name} - ${product.price}€ - <button onclick="removeProduct(${index})">Rimuovi</button>`;
                    productList.appendChild(li);
                });
            }
            
            window.removeProduct = function(index) {
                products.splice(index, 1);
                updateProductList();
            };
            
            window.sendMessage = function() {
                const message = chatInput.value.trim();
                if (message) {
                    messages.push(message);
                    chatInput.value = '';
                    updateChat();
                }
            };
            
            function updateChat() {
                chatWindow.innerHTML = '';
                messages.forEach(msg => {
                    const p = document.createElement('p');
                    p.textContent = msg;
                    chatWindow.appendChild(p);
                });
            }
        });
    </script>
</body>
</html>
